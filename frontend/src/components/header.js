import React from 'react';

const Header = () => {
  return (
    <header className="bg-primary text-white text-center p-3">
      Solar Sales Conversion Hub
    </header>
  );
};

export default Header;